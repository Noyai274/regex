import React from "react"
import { Link } from "react-router-dom"

export const Header=()=>{

    

    return(
        <div className="header-container">
            <Link to="/">Regex generator</Link>
        </div>
    )
}